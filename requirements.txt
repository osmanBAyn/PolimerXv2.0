# POLSEN app (app.py) -- core dependencies for deployment.
# Generated from the working PolimerXv4 venv. numpy pinned to a stable release
# (the dev env had a release candidate, which some hosts won't install).
streamlit==1.52.1
numpy>=2.0,<2.4
pandas==2.3.3
scikit-learn==1.8.0
joblib==1.5.2
xgboost==3.1.2
lightgbm==4.6.0
rdkit==2025.9.3
selfies==2.2.0
deap==1.4.3
datasets==4.4.1
huggingface-hub==0.36.0
pubchempy==1.0.5
stmol==0.0.9
py3Dmol==2.5.3
# stmol pins ipywidgets==7.6.3, which IMPORTS ipython_genutils but does not DECLARE it,
# so pip never installs it and the app dies with ModuleNotFoundError at import time.
ipywidgets==7.6.3
ipython_genutils==0.2.0
fpdf==1.7.2
plotly==6.5.0
matplotlib==3.10.8
requests==2.32.5

# --- OPTIONAL: only needed if you enable the heavy features in app.py ---
# Uncomment ONLY if you set USE_T5_RETRO = True in app.py. torch alone is ~2.8 GB, which
# dominates a container image; the rule-based retro engine does not need it.
# torch
# transformers
# LLM chatbot / AI interpretation (Gemini) -- uncomment if you enable the Gemini features:
# google-generativeai
